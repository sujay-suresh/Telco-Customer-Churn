# Models Directory

This directory stores trained machine learning models.

Saved models should use formats like:
- `.pkl` or `.joblib` for scikit-learn models
- `.h5` for Keras/TensorFlow models
- `.pt` or `.pth` for PyTorch models
